# SmartRetailX API Backend Base URL
# Leave blank during local development so Vite's dev proxy forwards requests without CORS issues.
# For production Docker/ALB, leaving blank routes requests to the same origin ALB.
VITE_API_BASE_URL=
VITE_APP_TITLE=SmartRetailX E-Commerce
